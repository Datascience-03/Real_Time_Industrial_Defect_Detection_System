fastapi>=0.95
uvicorn>=0.22
ultralytics>=8.4
onnxruntime>=1.17
opencv-python>=4.7
numpy>=1.24
matplotlib>=3.7
Pillow>=10.0
albumentations>=1.3

# NOTE: Install `torch` appropriate for your platform.
# For CPU-only environments, install the CPU wheels from PyTorch.
# For GPU environments, install CUDA-enabled PyTorch and replace `onnxruntime`
# with `onnxruntime-gpu`.
# Examples (do NOT hardcode here — choose per-host):
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
#   pip install onnxruntime-gpu

# Utilities
python-multipart
prometheus-client
requests
tqdm

# Test client used by FastAPI/Starlette integration tests.
httpx>=0.23
pytest>=8.0

# Optional: TensorRT Python bindings are system-specific and usually installed
# via NVIDIA packages on the host or inside an NVIDIA-provided container.
